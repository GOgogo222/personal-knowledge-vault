
我尝试用一两句话句话总结每节课讲了什么。。。

# 2-2-1 对象（控件）

对象（widgets），如一个按钮，标签，列表。。。

[什么是句柄？指针和句柄的区别-CSDN博客](https://blog.csdn.net/perfectguyipeng/article/details/71189822)

每个widget有其属性，如位置，大小，样式等等。。。特殊的widget还有其特殊属性，最小，大值，当前值

新建网页是一个很好的例子，想象浏览器是一个父类，而网页就是它的孩子

**这节课抛出了对象（Objects）的概念，用浏览器和网页的关系讲述了父类和子类的关系，带过图层的概念**

# 2-3-1 基础对象

基础对象（浏览器）是怎么创建的？展示了创建基础对象的函数

在vscode中修改，在Code::Blocks中编译运行

LVGL的三层屏幕：lv_screen_active(活动屏幕) < lv_layer_top(顶层) < lv_layer_sys(系统层)

创建一个对象的时候，要选择创建在哪一个图层（三选一）

“对象树讲的这么绕，lvgl一共有四颗对象树，优先级是父子，弟兄” 
*——弹幕*

演示了不同或相同图层的对象相互重叠的情况

obj = object!

**主要讲的是一个对象的创建过程，调用过程以及对象在不同图层上的效果**

**小结：图层很好理解，不过这里是在用代码演示；一个对象的诞生和调用可以类比new的用法，都是先开辟一段地址，然后使用指针进行调用**

# 2-4-1 对象的大小

介绍一些设置宽，高的函数

参数的计算会被搁置一段时间，得加上 lv_obj_update_layout(obj) 函数，get_width/get_height 函数才有意义

content_width/height ：实际可用的宽和高

**如何设置对象的大小，如何获取对象的大小，了解实际可用大小**

# 2-5-1 对象的位置

坐标原点默认在画布的左上角顶点 ①

**LVGL坐标系/LCD坐标系**与笛卡尔数学直角坐标系不同，其x轴向右为正方向，y轴向下为正方向 ②

① ② 可得：LVGL坐标系的坐标原点在左上角

最简单的对齐函数

对齐，我把它看作设置顶点的一种方式

对于参数的更新我可以这样理解，**参数获取函数**是有滞后性的，就是说我们在给参数赋值之后，需要使用update_layout函数告诉参数获取函数“你去重新检查一下，参数可能发生了变化，你要输出那个最新的值”

**对象坐标属性的设置，了解LVGL坐标，知道坐标原点的位置，知道对齐操作**

对齐操作其实可以看成改变坐标原点的一种形式

# 2-6-1 盒子模型（Boxing model）

网页搜索的图片界面可以看作是一个盒子模型——让大小不一的图片也能排版整齐

盒子模型主要有以下几个属性：content  padding  border  margin

- content：内容，可以是图片，文字或者其他盒子，若其他盒子太大就会产生滚动条
- padding：填充物，有软有硬
- border：有大小和颜色属性，真正意义上的**外壳**
- margin：盒子之间的间距

[深理解样式（Style）机制与应用方法_lvgl style](https://blog.csdn.net/weixin_52342399/article/details/146216851)

浅理解一下样式，例如一个白色按钮，按下时变为黑色，那么可以说这个按钮经过变化
样式1（state1）-> 样式2（state2），所以样式1的颜色属性为白，而样式2的颜色属性
为黑

样式是一个 **lv_style_t** 变量，而这个变量是一个自定义的**类**，类中由成员属性颜色，边框，文本等

**可以创建一个盒子，编译运行，直接看属性代表了盒子的哪一部分，这样会更直观**

# 2-7-1 样式（styles）

以一个进度条滑杆为例

侧重于从内存空间和地址方面来讲解一个样式的创立

多个对象可以共用一种样式
一个对象可以承载多种样式

一个滑杆的指针：指示当前值的填充区域（进度条）

LV_PART_MAIN（矩形），LV_PART_KNOB（旋钮），LV_PART_INDICATOR（指针）

删除某部分或所有样式的remove函数

两种样式：共享样式和私有样式
共享样式能节约空间，私有样式能打造“私有”组件
[Gemini](https://gemini.google.com/share/2088240f4900)

**介绍了样式，样式是一个自定义的类，其中的成员属性用来修饰一个组件的大小，颜色，边框等等**

# 2-8-1 事件（events）

临时变量容易被销毁，需要创建全局或静态（static）变量

